// Builder.io custom components are now registered directly
// in the pages/[...app].vue file using the modern customComponents prop approach
// This file is kept for backwards compatibility but no longer needed

export default defineNuxtPlugin(() => {
  // No longer needed - custom components are registered in the Content component directly
});
